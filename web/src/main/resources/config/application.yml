debug: true
spring:
      datasource:
                url: jdbc:mysql://localhost/searchanswer
                username: root
                password: Passw0rd
                driver-class-name: com.mysql.jdbc.Driver

      jpa:
         hibernate:
                  ddl-auto: update

         properties:
                   hibernate:
                           dialect: org.hibernate.dialect.MySQLDialect
         show-sql: true
      jackson:
             serialization:
                      indent_output: true
      driver-class-name: com.mysql.jdbc.Driver

server:
      port: 8010
      context-path: /searchAnswer