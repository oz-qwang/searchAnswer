debug: true
spring:
      datasource:
                url: jdbc:mysql://localhost/searchanswer?useUnicode=true&amp;characterEncoding=UTF-8
                username: root
                password: Passw0rd
                driver-class-name: com.mysql.jdbc.Driver

      jpa:
         hibernate:
                  ddl-auto: update

         properties:
                   hibernate:
                           dialect: org.hibernate.dialect.MySQLDialect
         show-sql: true
      jackson:
             serialization:
                      indent_output: true
      driver-class-name: com.mysql.jdbc.Driver
      resources:
              static-locations: classpath:/META-INF/resources/,classpath:/resources/,classpath:/static/,classpath:/public/
server:
      port: 8010
      context-path: /searchAnswer

custom:
      uploadpath: /Users/xinyu wei/project/SearchAnswer/build